<% if target.is_a?(ActiveStorage::Attached::One) %>
  <%= render partial: 'com/attachments/file_upload_one', locals: { target: f.object.public_send(name), f: f } %>
<% else %>
  <%= render partial: 'com/attachments/file_upload', locals: { target: f.object.public_send(name), f: f } %>
<% end %>
