<main class="columns container">
  <div class="column is-narrow" style="width: 260px;">
    <%= render 'nav' %>
  </div>
  <turbo-frame id="body" refresh="morph" data-turbo-action="advance" class="column">
    <div class="breadcrumb has-succeeds-separator">
      <ul>
        <%= render 'breadcrumb' %>
      </ul>
    </div>
    <%= yield %>
  </turbo-frame>
</main>