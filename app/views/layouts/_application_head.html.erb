<!DOCTYPE html>
<html lang="<%= I18n.locale %>" data-theme="light">
  <head>
    <%= render 'head_title' %>
    <%= render 'head_analysis' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="turbo-cache-control" content="no-preview">
    <meta name="turbo-prefetch" content="<%= !Rails.env.local? %>">
    <%= action_cable_meta_tag %>
    <link rel="icon" href="/icon.png" type="image/png">
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
    <%= render 'head_extra_js' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload', as: 'style' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= render 'head_templates' %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
