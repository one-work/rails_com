<%= render layout: 'layouts/admin_head' do %>
  <main class="columns">
    <div class="column is-narrow p-0 has-page">
      <%= render 'nav' %>
      <%= render 'menu_bar' %>
    </div>
    <turbo-frame id="body" data-turbo-action="advance" autoscroll data-autoscroll-block="start" class="column table-container pt-0">
      <%= render 'nav_bar' %>
      <%= render 'alert' %>
      <%= yield %>
    </turbo-frame>
  </main>
<% end %>
