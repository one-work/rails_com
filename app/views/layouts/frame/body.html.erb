<turbo-frame id="body" title="<%= current_title %>">
  <div class="breadcrumb has-succeeds-separator ml-3">
    <ul>
      <%= render 'breadcrumb' %>
    </ul>
  </div>
  <%= render 'alert' %>
  <div class="box as-body">
    <%= yield %>
  </div>
</turbo-frame>
