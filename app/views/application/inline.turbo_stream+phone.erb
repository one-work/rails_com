<%=
  turbo_stream.append(
    'body',
    partial: params.fetch(:partial, 'edit_inline_form'),
    locals: {
      model: defined?(model) ? model : instance_variable_get("@#{model_name}")
    }
  )
%>
