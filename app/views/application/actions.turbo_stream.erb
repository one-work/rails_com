<% if defined? model %>
  <%= turbo_stream.apend_body partial: 'actions', layout: 'modal_grids', locals: { model: model } %>
<% else %>
  <%= turbo_stream.apend_body partial: 'actions', layout: 'modal_grids', locals: { model: instance_variable_get("@#{model_name}") } %>
<% end %>
