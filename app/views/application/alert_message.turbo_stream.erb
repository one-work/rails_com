<% if defined?(prepend) %>
  <%= turbo_stream.prepend prepend, partial: 'alert_message', locals: { message: message } %>
<% else %>
  <%= turbo_stream.update 'alert', partial: 'alert_message', locals: { message: message } %>
<% end %>
