<%= form_with theme: 'default_phone', model: model, scope: model_name, url: { action: 'update' }, state: 'redirect' do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit %>
<% end %>
