<div class="card">
  <div class="card-header">用户身份</div>
  <div class="card-content">
    <% if current_organ %>
      <%= render partial: 'organ', locals: { organ: current_organ } %>
    <% end %>
  </div>
</div>