<%=
  tr_tag(
    model,
    { href: url_for(controller: 'statistic_days', statistic_id: model.id, **request.GET), icon: 'eye', title: t('.statistic_days.index.title') },
    icons: ['show', 'destroy']
  )
%>
