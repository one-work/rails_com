<%=
  tr_tag(
    model,
    {
      href: url_for(controller: '/com/audios', action: 'show', id:model.id),
      icon: 'play',
      title: t('com.audios.show.title')
    },
    icons: ['show', 'destroy']
  ) { yield }
%>
