<%=
  tr_tag(
    model,
    {
      action: url_for(action: 'clean', id: model.id),
      method: 'delete',
      icon: 'trash-arrow-up',
      title: t('.clean.title'),
      confirm: t('.clean.confirm')
    },
    icons: ['destroy']
  ) { yield }
%>
