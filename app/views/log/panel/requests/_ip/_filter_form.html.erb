<%= form_with theme: 'search', model: Log::Request.new, url: { action: params[:action] } do |f| %>
  <div class="field-body">
    <%= f.text_field :ip %>
    <div class="field is-narrow">
      <%= f.submit %>
    </div>
  </div>
<% end %>
