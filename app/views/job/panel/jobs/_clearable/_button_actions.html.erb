<%= form_with url: { action: 'batch_destroy' }, method: :delete, class: 'button_to' do |f| %>
  <%= f.hidden_field :from_action, value: params[:action] %>
  <%= button_tag disabled: true, class: 'button is-danger' do %>
    <%= svg_tag 'trash-list' %>
    <span class="ml-1"><%= t('.batch_destroy.title') %></span>
  <% end %>
<% end %>
