<%=
  tr_tag(
    model,
    {
      href: url_for(controller: 'publication_tables', publication_id: model.id, **request.query_parameters.slice(:shard)),
      icon: 'sitemap',
      title: t('.publication_tables.index.title')
    }
  ) { yield }
%>
