<%= turbo_stream.replace "tr_#{@controller.identifier}", partial: 'show_controller', locals: { meta_controller: @controller, expand: true } %>
<%=
  turbo_stream.replace(
    @controller.namespace.tr_id(@controller.business_identifier),
    partial: 'show_namespace',
    locals: { meta_namespace: @controller.namespace, business_identifier: @controller.business_identifier, expand: true }
  )
%>
<%= turbo_stream.replace @controller.business.tr_id, partial: 'show_business', locals: { meta_business: @controller.business, expand: true } %>
